global:
  scrape_interval: 15s      # ดึงข้อมูลทุก 15 วินาที

scrape_configs:
  - job_name: "health-exporter"
    static_configs:
      - targets: ["host.docker.internal:9090"]
      # host.docker.internal = เครื่อง Mac/Windows ของเรา จาก container
